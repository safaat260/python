def count_prime(arr):
    total = 0

    for num in arr:
        count = 0

        for i in range(1, num + 1):
            if num % i == 0:
                count += 1

        if count == 2:
            total += 1

    print("Prime numbers =", total)

arr = [2, 3, 4, 5, 6, 7, 8]
count_prime(arr)
