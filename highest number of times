def highest(arr):
    max_count = 0
    element = 0

    for i in arr:
        count = arr.count(i)

        if count > max_count:
            max_count = count
            element = i

    print("Element =", element)
    print("Frequency =", max_count)

arr = [1, 2, 2, 3, 4, 2, 5, 3]
highest(arr)
